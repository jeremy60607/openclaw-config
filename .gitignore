# Secrets - NEVER commit these
.env
.env.local
.env.production

# OS files
.DS_Store
Thumbs.db

# Backup files that may contain secrets
*.bak
*.backup

# Backups (may contain sanitized but sensitive info)
backups/

# Node
node_modules/
